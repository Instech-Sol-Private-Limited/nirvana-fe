import { DiscussionLayout } from "@/layout/DiscussionLayout";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <DiscussionLayout>{children}</DiscussionLayout>;
};

export default Layout;
